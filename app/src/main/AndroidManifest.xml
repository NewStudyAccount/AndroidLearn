<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <!-- 权限声明 -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidLearn" >

        <!-- ========== Activity 注册 ========== -->
        <!-- 主 Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Activity 生命周期演示 -->
        <activity
            android:name=".activity.ActivityLifecycleDemo"
            android:exported="false" /> <!-- 数据传递演示 -->
        <activity
            android:name=".activity.DataPassDemo"
            android:exported="false" /> <!-- 接收数据 Activity -->
        <activity
            android:name=".activity.ReceiveDataActivity"
            android:exported="false" /> <!-- 选择用户 Activity -->
        <activity
            android:name=".activity.PickUserActivity"
            android:exported="false" />

        <!-- TODO 待办清单 -->
        <activity
            android:name=".todo.TodoActivity"
            android:exported="false" />

        <!-- RecyclerView 综合演示 -->
        <activity
            android:name=".activity.RecyclerDemoActivity"
            android:exported="false" /> <!-- ========== Service 注册 ========== -->
        <!-- 后台服务 -->
        <service
            android:name=".service.MyService"
            android:exported="false" /> <!-- 绑定服务 -->
        <service
            android:name=".service.MyBindService"
            android:exported="false" /> <!-- ========== BroadcastReceiver 注册 ========== -->
        <!-- 静态注册广播接收器（监听开机完成） -->
        <receiver
            android:name=".receiver.MyReceiver"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver> <!-- ========== ContentProvider 注册 ========== -->
        <!-- 自定义 ContentProvider -->
        <provider
            android:name=".provider.UserContentProvider"
            android:authorities="com.example.androidlearn.provider"
            android:exported="true" />

        <activity
            android:name=".collection.CollectionMainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".collection.CollectionAddActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>